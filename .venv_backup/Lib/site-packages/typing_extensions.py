# Minimal typing_extensions stub for interactions.py
# This provides the basic functionality needed

import typing
if hasattr(typing, 'get_origin'):
    get_origin = typing.get_origin
else:
    def get_origin(tp):
        return getattr(tp, '__origin__', None)

if hasattr(typing, 'get_args'):
    get_args = typing.get_args
else:
    def get_args(tp):
        return getattr(tp, '__args__', ())

# Import everything from typing
from typing import *

# Add any missing items that might be needed
if not hasattr(typing, 'Literal'):
    try:
        from typing_extensions import Literal
    except ImportError:
        # Create a stub
        class Literal:
            pass
